// 1.Declare duas variáveis e atribua valores a elas. Em seguida, imprima no console(terminal) a soma dos valores.

var num1 = 0;
var num2 = 0;

var soma = num1 + num2;
console.log(soma);

// 2.Declare uma variável e atribua um valor numérico a ela. Em seguida, caso o valor for maior que 10 imprimir no console verdadeiro, caso contrário exibir falso.

var num3 = 11;

var resultado = num3 > 10;
console.log(resultado);

// 3.Declare duas variáveis e atribua valores a elas. Em seguida, imprima no console se o valor da primeira é maior que o valor da segunda.

var num4 = 10;
var num5 = 8;

var resultado = num4 > num5; 
console.log(resultado);

// 4.Declare uma variável e atribua um valor numérico a ela. Em seguida, imprima no console se o valor é igual a 5. 

var num6 = 5;

var resultado = num6 === 5; 
console.log(resultado);

// 5.Declare duas variáveis e atribua valores a ela. Em seguida, imprima no console se o valor da primeira é igual ao valor da segunda.

var num7 = 10;
var num8 = 10;

var resultado = num7 === num8;
console.log(resultado);

// 6.Declare duas variáveis e atribua valores a elas. Em seguida, imprima no console o resultado da divisão delas. 

var num9 = 0;
var num10 = 7;

var resultado = num9 /= num10;
console.log(resultado);

// 7.Declare uma variável e atribua um valor numérico a ela. Em seguida, imprima no console se o valor é diferente de 10.

var a = 20;

var resultado = a != 10;
console.log(resultado);

// 8.Declare duas variáveis e atribua valores a elas. Em seguida, imprima no console se o valor da primeira é menor que o valor da segunda.

var b = 3;
var c = 9;

var resultado = b < c;
console.log(resultado);

// 9.Declare uma variável e atribua um valor numérico a ela. Em seguida, imprima no console se o valor multiplicado por 2. 

var d = 0;

var resultado = d *= 7 ;
console.log(resultado);

// 10.Declare duas variáveis e atribua valores a elas. Em seguida, imprima no console o resultado da primeira variável dividido pela segunda variável. 

var e = 0;
var f = 7;

var resultado = e /= f;
console.log(resultado);
