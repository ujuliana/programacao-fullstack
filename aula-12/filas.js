//Caixa do Banco: << 1,2,3,4,5

//FIFO -> First in - First out 

// const fila = [];

// fila.unshift(1);
// fila.unshift(2);
// fila.unshift(3);
// fila.unshift(4);
// fila.unshift(5);

// console.log(fila.pop());
// console.log(fila.pop());
// console.log(fila);

const construirFila = () => {
    const dados = [];

    const adicionarDado = valor => {
        
    }
}